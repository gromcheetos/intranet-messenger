version: "3.9"

services:
  oracle:
    image: gvenzl/oracle-xe:latest
    container_name: hkapp-messenger
    ports:
      - "1523:1521"
    environment:
      ORACLE_PASSWORD: "system123"
    volumes:
      - ./oracle-data:/opt/oracle/oradata
      - ./dumps:/opt/oracle/dumps